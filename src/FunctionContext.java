public class FunctionContext {
    int currentNum;
    int currentSum;
    int addr;

    public FunctionContext(int currentNum, int currentSum, int addr) {
        super();
        this.currentNum = currentNum;
        this.currentSum = currentSum;
        this.addr = addr;
    }
}